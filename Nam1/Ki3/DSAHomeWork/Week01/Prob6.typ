= Explain why the statement, "The running time of algorithm $A$ is at least $O(n^2)$" is meaningless

The sentence is meaningless because the term $O(n^2)$ represent the upper bound of an algorithm. So the lower bound of an algorithm can't be $O(n^2)$